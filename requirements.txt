# Minimal direct dependencies for RS-01, pinned to the versions the published
# numbers were computed with. Transitive dependencies resolve from these; the
# complete environment as it stood is captured in requirements-lock.txt.
#
# The lock file is a Windows freeze and contains pywinpty, which will not
# install on Linux or macOS. Use this file to install, the lock file to audit.

numpy==2.4.6
scipy==1.17.1
scikit-image==0.26.0
matplotlib==3.11.1

rasterio==1.4.4
rioxarray==0.19.0
xarray==2026.7.0
shapely==2.1.2

odc-stac==0.5.3
odc-geo==0.5.3
dask==2026.8.0

pystac-client==0.9.0
planetary-computer==1.0.0
requests==2.34.2
